# App\ViewModels

Page/dashboard data-assembly classes. A view model gathers everything a page or
Livewire screen needs (queried live for transactional data — never cached) and
exposes it as typed, ready-to-render properties/methods. Keeps controllers and
components thin. See CLAUDE.md → Architecture rules.
